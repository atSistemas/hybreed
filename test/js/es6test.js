import chai from 'chai';
chai.should();
import sinon from 'sinon';



import Login from '../../src/modules/login/index';



describe('FIRST ES6 TEST', () => {

  beforeEach(() => {
  });

  it('should return true', () => {
    true.should.be.true;
    let myNumber=1;
    myNumber.should.be.a('number');
  })
});
